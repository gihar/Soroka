# Руководство по установке

В этом руководстве описано, как развернуть Soroka с использованием Docker (рекомендуется) или непосредственно на локальной машине.

## Предварительные требования

-   **Docker** и **Docker Compose** (для развертывания через Docker)
-   **Python 3.11+** (для локального развертывания)
-   **FFmpeg**, установленный в системе

## 🐳 Развертывание через Docker (Рекомендуется)

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <repository-url>
    cd Soroka
    ```

2.  **Создайте конфигурацию:**
    ```bash
    cp env_example .env
    ```
    Отредактируйте `.env` и заполните ваши API ключи. Подробнее см. в разделе [Конфигурация](CONFIGURATION.md).

3.  **Соберите и запустите:**
    ```bash
    docker-compose up -d --build
    ```

4.  **Проверьте логи:**
    ```bash
    docker-compose logs -f
    ```

## 💻 Локальная установка

Если вы предпочитаете запускать без Docker (например, для разработки):

1.  **Установите системные зависимости:**
    -   **macOS**: `brew install ffmpeg`
    -   **Ubuntu/Debian**: `sudo apt-get install ffmpeg`

2.  **Создайте виртуальное окружение:**
    ```bash
    python3 -m venv venv
    source venv/bin/activate
    ```

3.  **Установите Python зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    *Примечание: Некоторые библиотеки, такие как `pyannote.audio` или `torch`, могут потребовать специфических шагов установки в зависимости от вашего оборудования (CUDA/MPS).*

4.  **Настройте:**
    ```bash
    cp env_example .env
    # Отредактируйте .env
    ```

5.  **Запустите:**
    ```bash
    python main.py
    ```

## 🍎 Оптимизация для Apple Silicon (M1/M2/M3)

Для оптимальной производительности на macOS с Apple Silicon:

1.  Убедитесь, что используете **Python 3.11** (native arm64).
2.  PyTorch должен быть установлен с поддержкой MPS (обычно автоматически в последних версиях).
3.  В `.env` установите:
    ```env
    DIARIZATION_DEVICE=mps
    ```
